# Trip AI 작업 지침

## 제품과 현재 범위

- 이 저장소는 여행자의 조건과 취향에 맞는 여행지와 일정을 추천하는 AI를 개발한다.
- 현재 구현 범위는 한국관광공사 TourAPI 제주 장소 수집, 데이터 검증, 지도용 데이터 생성, 정적 지도 탐색 UI다.
- 추천·랭킹·일정 최적화 기능은 아직 구현되지 않았다. 계획된 기능을 현재 동작처럼 가정하지 않는다.
- `docs/`는 요구사항, 설계, 알고리즘, 데이터 계약을 관리하는 기준 문서 위치다.

## 최소 컨텍스트 원칙

1. 작업을 시작할 때 `docs/README.md`를 먼저 읽는다.
2. 문서 색인에서 현재 작업과 직접 관련된 SPEC과 그 SPEC이 링크한 기준 문서만 읽는다.
3. 여러 영역에 걸친 변경이 아니면 `docs/` 전체나 저장소 전체를 무조건 읽지 않는다.
4. 같은 내용을 여러 문서에 복제하지 말고 기준 문서 하나를 정한 뒤 다른 문서에서는 링크한다.
5. 기존 SPEC을 찾을 때는 제목, 요구사항 ID, 관련 코드 경로를 우선 검색한다.

## 필수 SDD 절차

코드, 설정, 데이터 스키마, 알고리즘, 사용자 동작, 외부 인터페이스 또는 생성 산출물을 변경할 때 다음 절차를 지킨다.

1. `docs/README.md`에서 적용할 SPEC을 확인한다.
2. 진행 중인 관련 SPEC이 없으면 `docs/spec_template.md`를 복사해 다음 번호의 `docs/spec_NNN.md`를 만들고 색인에 등록한다. 번호는 재사용하지 않는다.
3. 구현 전에 SPEC에 배경, 목표와 비목표, 요구사항, 영향 범위, 승인 기준, 테스트 계획을 작성한다.
4. 사용자가 명시적으로 요청한 범위는 승인된 요구사항으로 볼 수 있다. 범위를 크게 바꾸는 미결정 사항은 임의로 확정하지 않는다.
5. 구현 판단은 활성 SPEC과 링크된 기준 문서를 근거로 한다. 둘이 충돌하면 승인된 활성 SPEC이 목표 동작을 정의한다.
6. 구현 중 설계가 달라져야 하면 코드를 계속 확장하기 전에 SPEC의 결정과 근거를 먼저 갱신한다.
7. 요구사항 ID와 승인 기준에 대응하는 검증을 수행한다.
8. 완료 전에 SPEC을 실제 구현과 동기화한다. 상태, 관련 코드, 테스트 결과, 알려진 제한, 설계 차이, 변경 이력을 기록한다.
9. 장기적으로 유효한 동작이 바뀌면 관련 기준 문서도 갱신하고 `docs/README.md` 색인을 확인한다.
10. 코드와 문서가 불일치하거나 승인 기준을 검증하지 못했다면 완료로 보고하지 않는다.

순수 조사, 설명, 코드 변경 없는 리뷰, 오탈자와 링크 수정은 새 SPEC 없이 기존 문서를 바로잡을 수 있다. 문서 체계나 제품 동작 자체를 바꾸는 문서 변경에는 SPEC이 필요하다.

## SPEC 작성 규칙

- 파일명은 `docs/spec_NNN.md`, 문서 ID는 `SPEC-NNN` 형식을 사용한다.
- 상태는 `Draft`, `Approved`, `In Progress`, `Implemented`, `Superseded` 중 하나다.
- 요구사항은 `REQ-NNN`, 승인 기준은 `AC-NNN`처럼 추적 가능한 ID를 붙인다.
- 사실, 결정, 가정, 미결정 사항을 구분한다.
- 구현되지 않은 목표 설계는 반드시 `미구현`, `계획`, `초안` 중 하나로 표시한다.
- 코드 경로와 테스트 명령은 저장소 루트 기준으로 기록한다.
- 비밀키, 토큰, 사용자 개인정보, 원본 위치 이력은 문서 예시에 넣지 않는다.

## 여행 추천 도메인 원칙

- 필수 제약과 선호 조건을 분리한다. 필수 제약을 점수로 상쇄하지 않는다.
- 추천 결과는 사용한 데이터 시점, 알고리즘 버전, 주요 추천 이유를 추적할 수 있어야 한다.
- 장소 정보가 없거나 오래되었으면 추측으로 채우지 않고 `unknown`과 폴백 정책을 사용한다.
- 경도와 위도 순서를 명시한다. TourAPI의 `mapx`는 경도, `mapy`는 위도다.
- 영업시간, 휴무, 가격, 날씨처럼 변하는 정보는 출처와 확인 시각 없이 확정적으로 표현하지 않는다.
- 추천 품질 변경에는 오프라인 평가 시나리오와 회귀 검증을 포함한다.
- 정확한 사용자 위치와 민감한 취향 정보는 필요한 최소 범위로만 처리한다.
- 추천 설명은 실제 점수와 필터 근거에서 생성하며 모델이 근거를 꾸며내게 하지 않는다.

## 현재 검증 명령

변경 범위에 맞는 명령만 실행하고 결과를 SPEC에 기록한다.

```powershell
python -m py_compile scripts/collect_tourapi_jeju.py
node --check scripts/build_map_ui_data.mjs
node --check map-ui/app.js
node --test tests/map_ui_labels.test.mjs
```

지도용 데이터를 다시 생성해야 하는 변경에서는 다음 명령과 생성 diff를 함께 확인한다.

```powershell
node scripts/build_map_ui_data.mjs
```
